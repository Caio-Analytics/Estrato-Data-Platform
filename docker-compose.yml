services:
  dagster:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - dagster_history:/app/.dagster_home
    environment:
      - DAGSTER_HOME=/app/.dagster_home

volumes:
  dagster_history:
